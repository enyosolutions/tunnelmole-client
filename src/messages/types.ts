const initialise = 'initialise';
const hostnameAssigned = 'hostnameAssigned';
const forwardedResponse = 'forwardedResponse';
const forwardedResponseStreamStart = 'forwardedResponseStreamStart';
const forwardedResponseStreamChunk = 'forwardedResponseStreamChunk';

export {
    initialise,
    hostnameAssigned,
    forwardedResponse,
    forwardedResponseStreamStart,
    forwardedResponseStreamChunk
};
